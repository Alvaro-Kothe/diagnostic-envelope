source("renv/activate.R")
# Configure plot generated with `pdf`.
grDevices::pdf.options(width = 4, height = 4, pointsize = 10)

# Configure display code options. (set lower width for better printing)
options(
  width = 70,
  renv.config.auto.snapshot = TRUE,
  asympDiag.plot.AD_envelope.colors = c("gray50", "black")
)
